<%- include('../partials/nav') %>

    <section class="book-details-section">
        <div class="book-img-status">
            <img src="<%= foundBorrow.image %>" width="300">
            <h3 style="align-self: center;">Owned by <%= foundBorrow.owner.username %></h3>
        </div>

        <div class="book-details">
            <h1><%= foundBorrow.title %></h1>
            <h1 style="color: rgb(69, 63, 63); font-size: 24px;">Book By <%= foundBorrow.author %></h1>

            <% foundBorrow.genres.forEach((item) => { %>
                <button class="genres"><%= item %></button>
            <% }) %>

            <p><%= foundBorrow.description %></p>
            <hr>

            <h2>Rating & Review</h2>
            <p>⭐<%= foundBorrow.rate %> by <%= foundBorrow.owner.username %></p>
            <p><%= foundBorrow.review %></p>
            <hr>

            <h2>Borrow</h2>
            <div class="owner-manage">
                <% if (!user) { %>
                    <p><a href="/auth/sign-in">Sign in</a> to borrow this book.</p>
                <% } else if (userBorrow && userBorrow.status === 'pending') { %>
                    <button disabled>Request Pending</button>
                <% } else if (userBorrow && userBorrow.status === 'borrowed') { %>
                    <form action="/to-borrow/<%= foundBorrow._id %>/return" method="POST">
                        <button type="submit">Return this Book</button>
                    </form>
                <% } else if (foundBorrow.isBorrowed) { %>
                    <p>This book is already borrowed by <%= activeBorrow ? activeBorrow.userId.username : 'someone' %></p>
                <% } else { %>
                    <form action="/to-borrow/<%= foundBorrow._id %>" method="POST">
                        <button type="submit">Borrow this Book</button>
                    </form>
                <% } %>
            </div>
        </div>
    </section>

</body>
</html>